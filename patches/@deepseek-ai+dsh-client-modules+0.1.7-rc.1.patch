diff --git a/node_modules/@deepseek-ai/dsh-client-modules/lib/client.js b/node_modules/@deepseek-ai/dsh-client-modules/lib/client.js
index bca32d9..2da7347 100644
--- a/node_modules/@deepseek-ai/dsh-client-modules/lib/client.js
+++ b/node_modules/@deepseek-ai/dsh-client-modules/lib/client.js
@@ -265,7 +265,26 @@ window.__ModuleLoader__.load({
 						await this.create(loader, id);
 					}));
 					await loader.await();
+					const failures = [];
+					const requiredFailures = [];
+					for (const row of this.desired.plugins) {
+						const entry = this.managed.get(row.id);
+						if (entry?.fiber?.state === ACTIVE) continue;
+						let error;
+						try {
+							if (entry?.fiber === void 0) throw entry?.failure ?? new Error(`client-modules: ${row.id} import failed (see console)`);
+							if (entry.fiber.state === FAILED) await entry.fiber.await();
+							throw new Error(`client-modules: ${row.id} is waiting for activation`);
+						} catch (cause) {
+							error = cause;
+						}
+						if (row.inject.includes("dsh-desktop-workbenches")) failures.push({ id: row.id, message: String(error) });
+						else requiredFailures.push(error);
+					}
+					if (requiredFailures.length === 1) throw requiredFailures[0];
+					if (requiredFailures.length > 1) throw new AggregateError(requiredFailures, "required client modules failed to activate");
 					for (const row of this.modules.manifest.modules) this.revisions.set(row.id, row.rev);
+					this.publish({ syncing: false, failures });
 				});
 			}
 			/**
@@ -354,7 +373,7 @@ window.__ModuleLoader__.load({
 				const options = { name: id };
 				const entryId = loader.ensureId(options);
 				try {
-					await loader.create(options);
+					await loader.create(options, null, Infinity, true);
 				} finally {
 					this.managed.set(id, loader.resolve(entryId));
 				}
diff --git a/node_modules/@deepseek-ai/dsh-client-modules/lib/index.js b/node_modules/@deepseek-ai/dsh-client-modules/lib/index.js
index b948b1b..197762d 100644
--- a/node_modules/@deepseek-ai/dsh-client-modules/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-client-modules/lib/index.js
@@ -283,7 +283,7 @@ function readSourceMap(clientPath) {
 /** Count generated lines while assembling indexed-map section offsets. */
 function newlineCount(value) {
 	let count = 0;
-	for (const char of value) if (char === "\n") count += 1;
+	for (let index = value.indexOf("\n"); index !== -1; index = value.indexOf("\n", index + 1)) count += 1;
 	return count;
 }
 /** Resolve section sources against their original per-plugin map URL before combo relocation. */
@@ -305,7 +305,8 @@ function comboSectionMap(resource, original) {
 }
 /** Map each generated line to the same line in a bundled JavaScript source. */
 function identitySectionMap(source, sourceUrl) {
-	const mappings = Array.from({ length: newlineCount(source) }, (_, index) => index === 0 ? "AAAA" : "AACA").join(";");
+	const lines = newlineCount(source);
+	const mappings = lines === 0 ? "" : `AAAA${";AACA".repeat(lines - 1)}`;
 	return {
 		version: 3,
 		names: [],
@@ -378,6 +379,16 @@ function buildCombo(records, sourceMapOf, revision) {
 		sourceMapBody: lazyBody(() => buildComboSourceMap(resources, sourceMapOf))
 	};
 }
+const recordComboCache = /* @__PURE__ */ new WeakMap();
+/** Reuse one-record combo metadata until the record's content revision changes. */
+function recordCombo(record, sourceMapOf) {
+	const cached = recordComboCache.get(record);
+	if (cached?.rev === record.entry.rev) return cached.artifact;
+	const artifact = buildCombo([record], sourceMapOf, record.entry.rev);
+	recordComboCache.set(record, { rev: record.entry.rev, artifact });
+	return artifact;
+}
+
 /** Add initial-load scheduling metadata to a combo artifact. */
 function buildBatch(phase, records, sourceMapOf) {
 	const artifact = buildCombo(records, sourceMapOf);
@@ -667,7 +678,7 @@ var ClientModuleRegistry = class extends Service {
 		}
 		const responses = new Map(batchResponses);
 		for (const record of this.table.values()) {
-			const artifact = buildCombo([record], this.readSourceMap, record.entry.rev);
+			const artifact = recordCombo(record, this.readSourceMap);
 			responses.set(artifact.url, responses.get(artifact.url) ?? this.responses.get(artifact.url) ?? {
 				body: artifact.scriptBody,
 				contentType: "text/javascript; charset=utf-8"
@@ -767,7 +778,15 @@ var ClientModuleRegistry = class extends Service {
 				attributes: {}
 			}).url : internal.resolveSync(loaderName, baseUrl, {}).url;
 		} catch {
-			return;
+			if (expectedPackageName === void 0) return;
+			try {
+				return {
+					path: createRequire(baseUrl).resolve(`${expectedPackageName}/package.json`),
+					packageName: expectedPackageName
+				};
+			} catch {
+				return;
+			}
 		}
 		return this.nearestPackage(moduleUrl, expectedPackageName);
 	}
