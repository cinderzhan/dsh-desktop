diff --git a/node_modules/@deepseek-ai/dsh-client-ui-settings-general/lib/client.js b/node_modules/@deepseek-ai/dsh-client-ui-settings-general/lib/client.js
index e0e1a1d..f086301 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-settings-general/lib/client.js
+++ b/node_modules/@deepseek-ai/dsh-client-ui-settings-general/lib/client.js
@@ -31,7 +31,8 @@ window.__ModuleLoader__.load({
 			const tag = document.createElement("style");
 			tag.dataset.plugin = "@deepseek-ai/dsh-client-ui-settings-general";
 			tag.dataset.pluginCss = tagId$4;
-			tag.textContent = css$4;
+			// Keep every settings section reachable when the menu exceeds the panel height.
+			tag.textContent = css$4 + "\n.VOzbGW_nav{min-height:0;overflow-y:auto;padding-bottom:22px}";
 			document.head.appendChild(tag);
 		}
 		var SettingsRoot_module_css_default = {
@@ -337,10 +338,8 @@ window.__ModuleLoader__.load({
 			const connectionState = useConnectionState((state) => state);
 			const previousConnectionState = (0, react.useRef)(connectionState);
 			const onboardingSteps = useOnboardingSteps((s) => s);
-			const onboardingActive = useSessions((state) => {
-				const main = Object.values(state.byId).find((session) => (session.retainedBy.mainView ?? 0) > 0);
-				return state.phase === "ready" && (main === void 0 || main.blank);
-			});
+			// DSH Desktop owns first-run onboarding independently of the restored Session.
+			const onboardingActive = useSessions((state) => state.phase === "ready");
 			const onboardingStep = onboardingActive ? onboardingSteps.find((step) => !completedOnboarding.has(step.id)) : void 0;
 			(0, react.useEffect)(() => {
 				if (onboardingActive) return;
