diff --git a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/client.js b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/client.js
index 580e6df..4dbd56d 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/client.js
+++ b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/client.js
@@ -122,6 +122,7 @@ window.__ModuleLoader__.load({
 			"status.compact.planReview": "计划待审",
 			"status.compact.answer": "待回答",
 			"status.completed": "已完成",
+			"status.unread": "未读",
 			"schedule.active": "有活动定时任务",
 			"hover.created": "创建于 {time}",
 			"hover.copied": "已复制",
@@ -230,6 +231,7 @@ window.__ModuleLoader__.load({
 			"status.compact.planReview": "Plan review",
 			"status.compact.answer": "Answer",
 			"status.completed": "Completed",
+			"status.unread": "Unread",
 			"schedule.active": "Has active scheduled task",
 			"hover.created": "Created {time}",
 			"hover.copied": "Copied",
@@ -530,6 +532,7 @@ window.__ModuleLoader__.load({
 			const currentGroup = current === void 0 ? void 0 : owningGroupKey(workspaces, current);
 			const groups = [];
 			for (const g of groupByWorkspace(list, workspaces, archived, rowState.archivedFilter, view.ungroupedOrder)) {
+				if (g.sessions.length === 0) continue;
 				const expanded = expandedGroups.has(g.key);
 				groups.push({
 					key: g.key,
@@ -704,10 +707,18 @@ window.__ModuleLoader__.load({
 					orderBy: "updated",
 					groupExpansion: {},
 					sessionOrderByAccount: {},
+					unreadSessionIds: [],
 					archivedFilter: "default"
 				}),
 				persist: "dsh.workspace.view.v5",
 				actions: {
+					markSessionUnread: (d, sessionId) => {
+						const unread = d.unreadSessionIds ?? (d.unreadSessionIds = []);
+						if (!unread.includes(sessionId)) unread.push(sessionId);
+					},
+					markSessionRead: (d, sessionId) => {
+						d.unreadSessionIds = (d.unreadSessionIds ?? []).filter((id) => id !== sessionId);
+					},
 					setGroupBy: (d, mode) => {
 						d.groupBy = mode;
 					},
@@ -802,6 +813,12 @@ window.__ModuleLoader__.load({
 			lifetime = new AbortController();
 			selection = (0, _deepseek_ai_dsh_client_store.createSnapshotStore)({}, { persist: { name: "dsh.sessions.current" } });
 			mainReference;
+			sessionOpener = null;
+			sessionStarter = null;
+			sessionReuseFilter = null;
+			sessionFilter = null;
+			sessionFilterUnsubscribe = null;
+			sessionVisibility = (0, _deepseek_ai_dsh_client_store.createSnapshotStore)(0);
 			/**
 			* @param ctx - Client root Context.
 			* @param directoryPicker - the directory-picking Remote namespace.
@@ -828,23 +845,23 @@ window.__ModuleLoader__.load({
 					};
 				}, "ui-workspace: Workspace navigation policy");
 			}
-			async connectWorkspace(workspaceId) {
+			async connectWorkspace(workspaceId, fresh = false) {
 				const workspace = this.workspaces.list.getSnapshot().items.find((item) => item.workspaceId === workspaceId);
 				if (workspace === void 0) throw new Error(`uiWorkspace.connectWorkspace: unknown workspace ${workspaceId}`);
 				const inflight = this.connecting.get(workspaceId);
 				if (inflight !== void 0) return inflight;
-				const attempt = this.reuseOrCreateBlank(workspace).finally(() => {
+				const attempt = this.reuseOrCreateBlank(workspace, fresh).finally(() => {
 					this.connecting.delete(workspaceId);
 				});
 				this.connecting.set(workspaceId, attempt);
 				return attempt;
 			}
-			reuseOrCreateBlank(workspace) {
+			reuseOrCreateBlank(workspace, fresh = false) {
 				const archived = this.workspaces.list.getSnapshot().archivedSessionIds;
 				const sessions = this.sessions.list.getSnapshot();
-				for (const id of sessions.ids) {
+				if (!fresh) for (const id of sessions.ids) {
 					const summary = sessions.byId[id];
-					if (summary === void 0 || !summary.blank || summary.cwd !== workspace.path || !workspace.sessionIds.includes(id) || archived.includes(id)) continue;
+					if (summary === void 0 || !this.isSessionVisible(summary.id) || !summary.blank || summary.cwd !== workspace.path || !workspace.sessionIds.includes(id) || archived.includes(id) || this.sessionReuseFilter?.(id) === false) continue;
 					return this.reuseBlank(workspace.workspaceId, id);
 				}
 				return this.sessions.create({ workspaceId: workspace.workspaceId });
@@ -860,14 +877,34 @@ window.__ModuleLoader__.load({
 					return this.sessions.create({ workspaceId });
 				}
 			}
-			openSession(target) {
+			registerSessionOpener(handler) {
+				if (this.sessionOpener !== null) throw new Error("uiWorkspace session opener already registered");
+				this.sessionOpener = handler;
+				let active = true;
+				return () => {
+					if (!active) return;
+					active = false;
+					if (this.sessionOpener === handler) this.sessionOpener = null;
+				};
+			}
+			registerSessionReuseFilter(handler) {
+				if (this.sessionReuseFilter !== null) throw new Error("uiWorkspace session reuse filter already registered");
+				this.sessionReuseFilter = handler;
+				return () => {
+					if (this.sessionReuseFilter === handler) this.sessionReuseFilter = null;
+				};
+			}
+			openSession(target, source = "explicit-session") {
+				if (typeof target === "string") this.view.markSessionRead(target);
+				if (typeof target === "string" && this.sessionOpener?.(target, source) === true) return;
 				this.replaceMain(target, this.lifetime.signal, "reveal");
 			}
-			async openWorkspace(workspaceId, beforeOpen) {
+			async openWorkspace(workspaceId, beforeOpen, fresh = false) {
 				const navigation = AbortSignal.any([this.ctx.layout.beginNavigation(), this.lifetime.signal]);
+				const knownSessions = new Set(this.sessions.list.getSnapshot().ids);
 				let sessionId;
 				try {
-					sessionId = await this.connectWorkspace(workspaceId);
+				sessionId = await this.connectWorkspace(workspaceId, fresh);
 				} catch (error) {
 					if (!navigation.aborted) this.notify({
 						kind: "createFailed",
@@ -876,6 +913,11 @@ window.__ModuleLoader__.load({
 					throw error;
 				}
 				if (navigation.aborted) return;
+				this.view.markSessionRead(sessionId);
+				if (this.sessionOpener?.(sessionId, "workspace", {
+					workspaceId,
+					created: !knownSessions.has(sessionId)
+				}) === true) return;
 				this.replaceMain(sessionId, navigation, "reveal", beforeOpen);
 			}
 			async forkSession(sessionId) {
@@ -884,7 +926,37 @@ window.__ModuleLoader__.load({
 					increaseTitle: true
 				});
 			}
+			registerSessionStarter(handler) {
+				if (this.sessionStarter !== null) throw new Error("uiWorkspace session starter already registered");
+				this.sessionStarter = handler;
+				let active = true;
+				return () => {
+					if (!active) return;
+					active = false;
+					if (this.sessionStarter === handler) this.sessionStarter = null;
+				};
+			}
+			registerSessionFilter(filter, subscribe) {
+				if (this.sessionFilter !== null) throw new Error("uiWorkspace session filter already registered");
+				this.sessionFilter = filter;
+				const invalidate = () => this.sessionVisibility.set(this.sessionVisibility.getSnapshot() + 1);
+				this.sessionFilterUnsubscribe = subscribe(invalidate);
+				invalidate();
+				let active = true;
+				return () => {
+					if (!active) return;
+					active = false;
+					this.sessionFilterUnsubscribe?.();
+					this.sessionFilterUnsubscribe = null;
+					if (this.sessionFilter === filter) this.sessionFilter = null;
+					invalidate();
+				};
+			}
+			isSessionVisible(sessionId) {
+				return this.sessionFilter?.(sessionId) ?? true;
+			}
 			startSession(workspaceId) {
+				if (this.sessionStarter?.(workspaceId) === true) return;
 				const workspace = this.workspaces.list.getSnapshot();
 				const sessions = this.sessions.list.getSnapshot();
 				const current = this.mainReference?.sessionId;
@@ -895,7 +967,7 @@ window.__ModuleLoader__.load({
 					this.clearMain();
 					return;
 				}
-				this.openWorkspace(target).catch((reason) => {
+				this.openWorkspace(target, void 0, true).catch((reason) => {
 					console.warn("new session failed:", reason);
 				});
 			}
@@ -906,6 +978,24 @@ window.__ModuleLoader__.load({
 			async unarchiveSession(sessionId) {
 				await this.workspaces.unarchiveSession(sessionId);
 			}
+			/** Release the main view before deleting its durable Session. */
+			async deleteSession(sessionId) {
+				if (this.mainReference?.sessionId === sessionId) {
+					this.clearMain();
+					const retention = this.sessions.retainInfo(sessionId);
+					if (retention.getSnapshot().referenceCount > 0) await new Promise((resolve) => {
+						let unsubscribe = () => {};
+						let timer;
+						const finish = () => { clearTimeout(timer); unsubscribe(); resolve(); };
+						unsubscribe = retention.subscribe(() => {
+							if (retention.getSnapshot().referenceCount === 0) finish();
+						});
+						timer = setTimeout(finish, 250);
+						if (retention.getSnapshot().referenceCount === 0) finish();
+					});
+				}
+				await this.sessions.delete(sessionId);
+			}
 			async pinSession(sessionId) {
 				await this.workspaces.pinSession(sessionId);
 				const { items, pinnedSessionIds, archivedSessionIds } = this.workspaces.list.getSnapshot();
@@ -1533,7 +1623,7 @@ window.__ModuleLoader__.load({
 		* @param props.t - Workspace-browser translation seat.
 		* @returns the result row.
 		*/
-		function SearchResultItem({ result, currentId, onOpen, onUnarchive, t }) {
+		function SearchResultItem({ result, currentId, onOpen, onUnarchive, renderSlot, t }) {
 			const selected = result.id === currentId;
 			const statuses = sessionStatuses(result, t);
 			const primaryStatus = statuses[0];
@@ -1548,6 +1638,7 @@ window.__ModuleLoader__.load({
 				children: [(0, react_jsx_runtime.jsxs)("span", {
 					className: Rows_module_css_default.searchResultHeading,
 					children: [
+						renderSlot("sidebar.session.leading", { sessionId: result.id, size: 14 }),
 						(0, react_jsx_runtime.jsx)("span", {
 							className: Rows_module_css_default.slot,
 							children: !result.archived && primaryStatus.state !== "idle" && (0, react_jsx_runtime.jsx)(SessionStatusDots, { statuses })
@@ -1608,7 +1699,7 @@ window.__ModuleLoader__.load({
 		* @param props.t - the browser root's locale seat.
 		* @returns the session row.
 		*/
-		function SessionNodeItem({ node, currentId, now, onOpen, onRenameRequest, renderSlot, onReveal, drag, flat = false, t }) {
+		function SessionNodeItem({ node, currentId, now, onOpen, onRenameRequest, renderSlot, onReveal, drag, flat = false, unread = false, onUnreadChange, t }) {
 			const row = node;
 			const title = displayTitle(node, t);
 			const selected = node.id === currentId;
@@ -1630,13 +1721,18 @@ window.__ModuleLoader__.load({
 				anchor: (0, react_jsx_runtime.jsxs)("div", {
 					ref: rowRef,
 					"data-row-key": `session:${node.id}`,
-					className: clsx(Rows_module_css_default.sessionRow, selected && Rows_module_css_default.selected, menuOpen && Rows_module_css_default.menuOpen, row.archived && Rows_module_css_default.archived, flat && !showStatus && Rows_module_css_default.flatSessionRowWithoutStatus, drag?.marker === "before" && Rows_module_css_default.dropBefore, drag?.marker === "after" && Rows_module_css_default.dropAfter),
+					className: clsx(Rows_module_css_default.sessionRow, selected && Rows_module_css_default.selected, menuOpen && Rows_module_css_default.menuOpen, row.archived && Rows_module_css_default.archived, flat && !showStatus && !unread && Rows_module_css_default.flatSessionRowWithoutStatus, drag?.marker === "before" && Rows_module_css_default.dropBefore, drag?.marker === "after" && Rows_module_css_default.dropAfter),
 					role: "treeitem",
 					"aria-selected": selected,
 					"aria-description": row.archived ? t("toast.archivedNotOpenable") : void 0,
 					onClick: () => {
 						onOpen(node.id);
 					},
+					onContextMenu: (event) => {
+						event.preventDefault();
+						event.stopPropagation();
+						if (!row.blank) setMenuOpen(true);
+					},
 					onPointerEnter: marquee.enter,
 					onPointerLeave: marquee.leave,
 					draggable,
@@ -1658,13 +1754,15 @@ window.__ModuleLoader__.load({
 						drag.drop(rowHalf(e));
 					},
 					children: [
-						(!flat || showStatus) && (0, react_jsx_runtime.jsx)("span", {
+						renderSlot("sidebar.session.leading", { sessionId: node.id, size: 14 }),
+						(!flat || showStatus || unread) && (0, react_jsx_runtime.jsx)("span", {
 							className: Rows_module_css_default.slot,
-							children: !row.archived && showStatus && (0, react_jsx_runtime.jsx)(SessionStatusDots, { statuses })
+							children: !row.archived && (showStatus ? (0, react_jsx_runtime.jsx)(SessionStatusDots, { statuses }) : unread ? (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [(0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.StateDot, { state: "done" }), (0, react_jsx_runtime.jsx)("span", { className: Rows_module_css_default.visuallyHidden, children: t("status.unread") })] }) : null)
 						}),
 						(0, react_jsx_runtime.jsx)("span", {
 							ref: titleRef,
 							className: Rows_module_css_default.title,
+							style: unread ? { fontWeight: 600 } : void 0,
 							onDoubleClick: row.blank ? void 0 : (e) => {
 								e.stopPropagation();
 								onRenameRequest(node.id, row.title);
@@ -1701,7 +1799,9 @@ window.__ModuleLoader__.load({
 								}),
 								children: renderSlot("sidebar.workspaces.session.menu.item", {
 									sessionId: node.id,
-									displayTitle: row.title
+									displayTitle: row.title,
+									unread,
+									onUnreadChange
 								}, { hookContext: menuOpenState })
 							}), renderSlot("sidebar.workspaces.session.row.action", {
 								sessionId: node.id,
@@ -2321,7 +2421,7 @@ window.__ModuleLoader__.load({
 			return e.clientY < rect.top + rect.height / 2 ? "before" : "after";
 		}
 		/** The scrolling session tree; unmounting drops the sessions subscription and local row limits. */
-		function SessionTree({ list, useSessionStatus, startSession, open, workspaces, ungroupedSessionIds, rowState, workspaceReady, animationResetKey, usePanelInfo, onRenameRequest, onDeleteRequest, onSessionRenameRequest, renderSlot, insertWorkspaceBefore, nestWorkspaces, groupExpansion, setGroupExpanded, setSessionOrder, home, t, revealSessionId, onSessionRevealed }) {
+		function SessionTree({ list, useSessionStatus, startSession, open, workspaces, ungroupedSessionIds, rowState, unreadSessionIds, onUnreadChange, workspaceReady, animationResetKey, usePanelInfo, onRenameRequest, onDeleteRequest, onSessionRenameRequest, renderSlot, insertWorkspaceBefore, nestWorkspaces, groupExpansion, setGroupExpanded, setSessionOrder, home, t, revealSessionId, onSessionRevealed }) {
 			const panelActive = usePanelInfo((info) => info.activePanelId !== null);
 			const statuses = useSessionStatus((s) => s);
 			const current = panelActive ? void 0 : Object.values(list.byId).find((session) => (session.retainedBy.mainView ?? 0) > 0)?.id;
@@ -2593,6 +2693,8 @@ window.__ModuleLoader__.load({
 								onOpen: open,
 								onRenameRequest: onSessionRenameRequest,
 								renderSlot,
+								unread: unreadSessionIds.includes(node.id),
+								onUnreadChange,
 								onReveal: node.id === revealSessionId && group.key === revealGroup ? () => {
 									onSessionRevealed(node.id);
 								} : void 0,
@@ -2641,7 +2743,7 @@ window.__ModuleLoader__.load({
 			});
 		}
 		/** The flat "In one list" body: every session is one draggable top-level row. */
-		function FlatList({ list, sessionIds, rowState, useSessionStatus, open, onSessionRenameRequest, renderSlot, usePanelInfo, setSessionOrder, workspaceReady, animationResetKey, revealSessionId, onSessionRevealed, t }) {
+		function FlatList({ list, sessionIds, rowState, unreadSessionIds, onUnreadChange, useSessionStatus, open, onSessionRenameRequest, renderSlot, usePanelInfo, setSessionOrder, workspaceReady, animationResetKey, revealSessionId, onSessionRevealed, t }) {
 			const panelActive = usePanelInfo((info) => info.activePanelId !== null);
 			const statuses = useSessionStatus((s) => s);
 			const rows = (0, react.useMemo)(() => deriveFlat(list, sessionIds, rowState, statuses), [
@@ -2682,8 +2784,10 @@ window.__ModuleLoader__.load({
 							currentId,
 							now,
 							onOpen: open,
-							onRenameRequest: onSessionRenameRequest,
-							renderSlot,
+								onRenameRequest: onSessionRenameRequest,
+								renderSlot,
+								unread: unreadSessionIds.includes(node.id),
+								onUnreadChange,
 							onReveal: node.id === revealSessionId ? () => {
 								onSessionRevealed(node.id);
 							} : void 0,
@@ -2728,9 +2832,15 @@ window.__ModuleLoader__.load({
 			});
 		}
 		/** Flat search body: local metadata matches plus the current Host result page. */
-		function SearchResults({ useSessions, useSessionStatus, open, onUnarchive, workspaces, archivedSessionIds, archivedFilter, query, remote, resultLimit, usePanelInfo, t }) {
+		function SearchResults({ useSessions, useSessionStatus, open, onUnarchive, workspaces, archivedSessionIds, archivedFilter, query, remote, resultLimit, usePanelInfo, isSessionVisible, useSessionVisibility, renderSlot, t }) {
 			const panelActive = usePanelInfo((info) => info.activePanelId !== null);
-			const list = useSessions((s) => s);
+			const sourceList = useSessions((s) => s);
+			const sessionVisibilityRevision = useSessionVisibility((revision) => revision);
+			const list = (0, react.useMemo)(() => {
+				const ids = sourceList.ids.filter(isSessionVisible);
+				const visible = new Set(ids);
+				return { ...sourceList, ids, byId: Object.fromEntries(Object.entries(sourceList.byId).filter(([id]) => visible.has(id))) };
+			}, [isSessionVisible, sessionVisibilityRevision, sourceList]);
 			const statuses = useSessionStatus((s) => s);
 			const currentRemote = remote.query === query ? remote : {
 				query,
@@ -2764,6 +2874,7 @@ window.__ModuleLoader__.load({
 								currentId,
 								onOpen: open,
 								onUnarchive,
+								renderSlot,
 								t
 							}, result.id))
 						}),
@@ -2796,10 +2907,17 @@ window.__ModuleLoader__.load({
 		* @param props - composed slot props (shell owner share + store + injected actions).
 		* @returns the region element tree.
 		*/
-		function WorkspaceBrowser({ wide, usePanelInfo, expandSidebar, useSessions, useSessionStatus, useWorkspaces, useStore, actions, startSession, open, requestSessionRename, notifyArchivedNotOpenable, renameWorkspace, deleteWorkspace, insertWorkspaceBefore, unarchiveSession, createWorkspace, searchSessions, searchResultLimit, useDirectoryFlow, useHostInfo, renderSlot, t }) {
+		function WorkspaceBrowser({ wide, usePanelInfo, expandSidebar, useSessions, useSessionStatus, useWorkspaces, useStore, actions, startSession, open, requestSessionRename, notifyArchivedNotOpenable, renameWorkspace, deleteWorkspace, insertWorkspaceBefore, unarchiveSession, createWorkspace, searchSessions, searchResultLimit, isSessionVisible, useDirectoryFlow, useHostInfo, useSessionVisibility, renderSlot, t }) {
 			const home = useHostInfo((info) => info.home);
-			const list = useSessions((state) => state);
-			const workspaces = useWorkspaces((state) => state.items);
+			const sourceList = useSessions((state) => state);
+			const sessionVisibilityRevision = useSessionVisibility((revision) => revision);
+			const list = (0, react.useMemo)(() => {
+				const ids = sourceList.ids.filter(isSessionVisible);
+				const visible = new Set(ids);
+				return { ...sourceList, ids, byId: Object.fromEntries(Object.entries(sourceList.byId).filter(([id]) => visible.has(id))) };
+			}, [isSessionVisible, sessionVisibilityRevision, sourceList]);
+			const sourceWorkspaces = useWorkspaces((state) => state.items);
+			const workspaces = (0, react.useMemo)(() => sourceWorkspaces.map((workspace) => ({ ...workspace, sessionIds: workspace.sessionIds.filter(isSessionVisible) })).filter((workspace) => workspace.sessionIds.length > 0), [isSessionVisible, sessionVisibilityRevision, sourceWorkspaces]);
 			const workspacePhase = useWorkspaces((state) => state.phase);
 			const workspaceStreamState = useWorkspaces((state) => state.state);
 			const archivedSessionIds = useWorkspaces((state) => state.archivedSessionIds);
@@ -2810,6 +2928,11 @@ window.__ModuleLoader__.load({
 			const archivedFilter = useStore((s) => s.archivedFilter ?? "default");
 			const groupExpansion = useStore((s) => s.groupExpansion);
 			const sessionOrderByAccount = useStore((s) => s.sessionOrderByAccount);
+			const unreadSessionIds = useStore((s) => s.unreadSessionIds ?? []);
+			const onUnreadChange = (sessionId, unread) => {
+				if (unread) actions.markSessionUnread(sessionId);
+				else actions.markSessionRead(sessionId);
+			};
 			const guardedOpen = (sessionId) => {
 				if (archivedSessionIds.includes(sessionId)) {
 					notifyArchivedNotOpenable();
@@ -3239,12 +3362,17 @@ window.__ModuleLoader__.load({
 							query: normalizedQuery,
 							remote: remoteSearch,
 							resultLimit: searchResultLimit,
+							isSessionVisible,
+							useSessionVisibility,
+							renderSlot,
 							t
 						}) : groupBy === "flat" ? (0, react_jsx_runtime.jsx)(FlatList, {
 							usePanelInfo,
 							list,
 							sessionIds: orderedFlatSessionIds,
 							rowState,
+							unreadSessionIds,
+							onUnreadChange,
 							workspaceReady,
 							animationResetKey: `${groupBy}/${orderBy}/${archivedFilter}`,
 							useSessionStatus,
@@ -3270,6 +3398,8 @@ window.__ModuleLoader__.load({
 							setGroupExpanded: actions.setGroupExpanded,
 							setSessionOrder: saveSessionOrder,
 							rowState,
+							unreadSessionIds,
+							onUnreadChange,
 							startSession,
 							open: guardedOpen,
 							insertWorkspaceBefore,
@@ -3992,6 +4122,7 @@ window.__ModuleLoader__.load({
 				open: openSession,
 				searchSessions,
 				searchResultLimit: sessions.searchResultLimit,
+				isSessionVisible: (sessionId) => uiWorkspace.isSessionVisible(sessionId),
 				requestSessionRename,
 				notifyArchivedNotOpenable: () => {
 					notify({ kind: "archivedNotOpenable" });
@@ -4009,10 +4140,11 @@ window.__ModuleLoader__.load({
 					await uiWorkspace.unarchiveSession(sessionId);
 				},
 				createWorkspace: (input) => workspaces.create(input),
-				hooks: {
-					directoryFlow: browserFlowSource,
-					hostInfo
-				}
+					hooks: {
+						directoryFlow: browserFlowSource,
+						hostInfo,
+						sessionVisibility: uiWorkspace.sessionVisibility
+					}
 			});
 			const pickerInjected = () => ({
 				createWorkspace: (input) => workspaces.create(input),
@@ -4033,6 +4165,10 @@ window.__ModuleLoader__.load({
 					"sidebar.workspaces.session.row.action": {
 						kind: "list",
 						scope: "root"
+					},
+					"sidebar.session.leading": {
+						kind: "single",
+						scope: "root"
 					}
 				},
 				store: viewStore,
diff --git a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/contract/slots.d.ts b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/contract/slots.d.ts
index 5ca7ad9..2c478d4 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/contract/slots.d.ts
+++ b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/contract/slots.d.ts
@@ -148,6 +148,11 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
             scope: 'root';
             owner: SessionRowOwnerProps;
         };
+        'sidebar.session.leading': {
+            kind: 'single';
+            scope: 'root';
+            owner: { sessionId: SessionId; size?: number };
+        };
     }
 }
 /** The two directory-flow holes; a flow package's client half registers its one component into both. */
@@ -181,6 +186,7 @@ export type WorkspaceBrowserInjected = {
          * saw. Select the field the surface needs (`info => info.home`).
          */
         hostInfo: HostObservable<RemoteHostFacts>;
+        sessionVisibility: HostObservable<number>;
     };
     /**
      * Start a New Session in a Workspace: reuse-or-create its blank session and
@@ -200,6 +206,7 @@ export type WorkspaceBrowserInjected = {
     }>;
     /** Maximum number of merged rows rendered for one search. */
     searchResultLimit: number;
+    isSessionVisible: (sessionId: SessionId) => boolean;
     /** Open the Session rename dialog (a row title double-click); the rename action entry raises the same request. */
     requestSessionRename: (sessionId: SessionId, currentTitle: string) => void;
     /** Tell the user an archived row cannot be opened (a click on it). */
@@ -373,7 +380,7 @@ export type SessionArchiveConfirmProps = PropsRuntime<'shell.overlay'> & PropsLo
 /** Props of the row toast entry in `shell.overlay`. */
 export type RowToastProps = PropsRuntime<'shell.overlay'> & PropsLocale<'workspace'> & Omit<RowToastInjected, 'hooks'> & PropsHooks<RowToastInjected['hooks']>;
 /** Full browser props: shell owner share + viewing store + injected actions + the locale seat. */
-export type WorkspaceBrowserProps = PropsRuntime<'sidebar.workspaces'> & PropsRenderSlots<'sidebar.workspaces.directoryFlow' | 'sidebar.workspaces.session.menu.item' | 'sidebar.workspaces.session.row.action'> & PropsStore<WorkspaceViewStoreHandle> & Omit<WorkspaceBrowserInjected, 'hooks'> & PropsHooks<WorkspaceBrowserInjected['hooks']> & PropsLocale<'workspace'>;
+export type WorkspaceBrowserProps = PropsRuntime<'sidebar.workspaces'> & PropsRenderSlots<'sidebar.workspaces.directoryFlow' | 'sidebar.workspaces.session.menu.item' | 'sidebar.workspaces.session.row.action' | 'sidebar.session.leading'> & PropsStore<WorkspaceViewStoreHandle> & Omit<WorkspaceBrowserInjected, 'hooks'> & PropsHooks<WorkspaceBrowserInjected['hooks']> & PropsLocale<'workspace'>;
 /**
  * Picker-private injected share. Pick semantics remain in the owner's onPick
  * callback; this callback creates only the real Host Workspace. A type alias
diff --git a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/locales.d.ts b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/locales.d.ts
index ade79bb..feef33e 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/locales.d.ts
+++ b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/locales.d.ts
@@ -99,6 +99,7 @@ export declare const zh: {
     'status.compact.planReview': string;
     'status.compact.answer': string;
     'status.completed': string;
+    'status.unread': string;
     'schedule.active': string;
     'hover.created': string;
     'hover.copied': string;
@@ -209,6 +210,7 @@ export declare const en: {
     'status.compact.planReview': string;
     'status.compact.answer': string;
     'status.completed': string;
+    'status.unread': string;
     'schedule.active': string;
     'hover.created': string;
     'hover.copied': string;
diff --git a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/navigation.d.ts b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/navigation.d.ts
index e251cf2..3d01974 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/navigation.d.ts
+++ b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/navigation.d.ts
@@ -4,6 +4,11 @@ import type { ClientRemote, DirectoryListing, RemoteFailure } from '@deepseek-ai
 import type { ISessions, SessionTarget } from '@deepseek-ai/dsh-api-session-controller/client';
 import type { IWorkspaces, WorkspaceId } from '@deepseek-ai/dsh-api-workspace-controller/client';
 import type { SessionId } from '@deepseek-ai/dsh-session/types';
+export type SessionNavigationSource = 'explicit-session' | 'workspace';
+export interface SessionNavigationContext {
+    workspaceId: WorkspaceId;
+    created: boolean;
+}
 import type { RowToast } from './contract/slots.ts';
 import type { WorkspaceViewStoreActions } from './stores.ts';
 /** Workspace archive and directory operations consumed by Client UI domains. */
@@ -12,7 +17,11 @@ export interface UiWorkspace {
      * Select a Session and show its Conversation as one UI navigation action.
      * @param target - known Session identity or durable direct-parent subagent address to display.
      */
-    openSession(target: SessionTarget): void;
+    registerSessionOpener(handler: (sessionId: SessionId, source?: SessionNavigationSource, context?: SessionNavigationContext) => boolean): () => void;
+    registerSessionReuseFilter(handler: (sessionId: SessionId) => boolean): () => void;
+    registerSessionFilter(filter: (sessionId: SessionId) => boolean, subscribe: (listener: () => void) => () => void): () => void;
+    isSessionVisible(sessionId: SessionId): boolean;
+    openSession(target: SessionTarget, source?: SessionNavigationSource): void;
     /**
      * Connect a Workspace and open its Session unless a later navigation supersedes it.
      * @param workspaceId - target Workspace.
@@ -21,24 +30,26 @@ export interface UiWorkspace {
      * @throws on failure; a refused creation is also shown through the Workspace
      * notice unless a later navigation or disposal superseded the request.
      */
-    openWorkspace(workspaceId: WorkspaceId, beforeOpen?: (sessionId: SessionId) => void): Promise<void>;
+    openWorkspace(workspaceId: WorkspaceId, beforeOpen?: (sessionId: SessionId) => void, fresh?: boolean): Promise<void>;
     /**
      * Fork a Session without changing the current selection.
      * @param sessionId - source Session.
      * @returns completion after child creation and inherited-title increment.
      */
     forkSession(sessionId: SessionId): Promise<void>;
+    deleteSession(sessionId: SessionId): Promise<void>;
     /**
      * Resolve the reusable or newly created blank Session for a Workspace.
      * @param workspaceId - target Workspace.
      * @returns a Session already addressable through the Session Controller.
      */
-    connectWorkspace(workspaceId: WorkspaceId): Promise<SessionId>;
+    connectWorkspace(workspaceId: WorkspaceId, fresh?: boolean): Promise<SessionId>;
     /**
      * Start a New Session flow and navigate to its Session; a creation the Host
      * refuses is shown through the Workspace notice and leaves the selection as it was.
      * @param workspaceId - explicit target; absent inherits the current or most recent Workspace.
      */
+    registerSessionStarter(handler: (workspaceId?: WorkspaceId) => boolean): () => void;
     startSession(workspaceId?: WorkspaceId): void;
     /**
      * Archive a Session and clear it when it is the current selection.
@@ -110,6 +121,12 @@ declare class UiWorkspaceService extends Service implements UiWorkspace {
     private readonly lifetime;
     private readonly selection;
     private mainReference;
+    private sessionOpener;
+    private sessionStarter;
+    private sessionReuseFilter;
+    private sessionFilter;
+    private sessionFilterUnsubscribe;
+    readonly sessionVisibility;
     /**
      * @param ctx - Client root Context.
      * @param directoryPicker - the directory-picking Remote namespace.
@@ -118,13 +135,19 @@ declare class UiWorkspaceService extends Service implements UiWorkspace {
      * @param view - the browser's viewing-store write set (one instance shared with its registration).
      * @param notify - show one notice through the Workspace notice channel.
      */
-    constructor(ctx: Context, directoryPicker: ClientRemote['directoryPicker'], workspaces: IWorkspaces, sessions: ISessions, view: Pick<WorkspaceViewStoreActions, 'pinSessionOrder'>, notify: (toast: RowToast) => void);
-    connectWorkspace(workspaceId: WorkspaceId): Promise<SessionId>;
+    constructor(ctx: Context, directoryPicker: ClientRemote['directoryPicker'], workspaces: IWorkspaces, sessions: ISessions, view: Pick<WorkspaceViewStoreActions, 'pinSessionOrder' | 'markSessionRead'>, notify: (toast: RowToast) => void);
+    connectWorkspace(workspaceId: WorkspaceId, fresh?: boolean): Promise<SessionId>;
     private reuseOrCreateBlank;
     private reuseBlank;
-    openSession(target: SessionTarget): void;
-    openWorkspace(workspaceId: WorkspaceId, beforeOpen?: (sessionId: SessionId) => void): Promise<void>;
+    registerSessionOpener(handler: (sessionId: SessionId, source?: SessionNavigationSource, context?: SessionNavigationContext) => boolean): () => void;
+    registerSessionReuseFilter(handler: (sessionId: SessionId) => boolean): () => void;
+    registerSessionFilter(filter: (sessionId: SessionId) => boolean, subscribe: (listener: () => void) => () => void): () => void;
+    isSessionVisible(sessionId: SessionId): boolean;
+    openSession(target: SessionTarget, source?: SessionNavigationSource): void;
+    openWorkspace(workspaceId: WorkspaceId, beforeOpen?: (sessionId: SessionId) => void, fresh?: boolean): Promise<void>;
     forkSession(sessionId: SessionId): Promise<void>;
+    deleteSession(sessionId: SessionId): Promise<void>;
+    registerSessionStarter(handler: (workspaceId?: WorkspaceId) => boolean): () => void;
     startSession(workspaceId?: WorkspaceId): void;
     archiveSession(sessionId: SessionId, options?: {
         readonly stopActivity?: boolean;
diff --git a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/stores.d.ts b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/stores.d.ts
index 4e20866..d4d260f 100644
--- a/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/stores.d.ts
+++ b/node_modules/@deepseek-ai/dsh-client-ui-workspace/lib/types/client/stores.d.ts
@@ -23,6 +23,8 @@ type WorkspaceViewState = {
     groupExpansion: Record<string, boolean>;
     /** Saved manual order per Workspace group plus the browser-local flat-list account. */
     sessionOrderByAccount: Record<string, string[]>;
+    /** Desktop unread markers; absent in older persisted v5 snapshots. */
+    unreadSessionIds?: string[];
     /** Archived-row visibility; omitted in pre-filter v5 snapshots and read as 'default'. */
     archivedFilter?: ArchivedFilter;
 };
@@ -36,6 +38,8 @@ type SessionOrderSource = {
  * return type); drift fails assignability at the defineStore call.
  */
 type WorkspaceViewActions = {
+    markSessionUnread: (draft: WorkspaceViewState, sessionId: string) => void;
+    markSessionRead: (draft: WorkspaceViewState, sessionId: string) => void;
     setGroupBy: (draft: WorkspaceViewState, mode: SessionGroupBy) => void;
     setOrderBy: (draft: WorkspaceViewState, mode: SessionOrderBy, initialOrders: Readonly<Record<string, readonly string[]>>) => void;
     setGroupExpanded: (draft: WorkspaceViewState, key: string, expanded: boolean) => void;
